<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  
     <% if Helpers.is_logged_in?(@user) %>
       <h1>Welcome <%=Helpers.current_user(@user).username%></h1> 
       <h3>Your Balance: <%=Helpers.current_user(@user).balance%></h3>
      <%else%>%
       <a href="/index">Home Page</a>
     <%end%>
    
    <form method="post" action="">
      <a href= '/logout'>Log Out</a>
    </form>

  </body>
</html> 