<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    
    <form method="post" action="">
     <% if Helpers.is_logged_in?(@user[:id]) %>
     <h1>Welcome <%=Helpers.current_user(@user).username%></h1>
     <h3>Balance: <%=Helpers.current_user(@user).balance%></h3>
     <%end%>
    <a href= '/logout'>Log Out</a>
    </form>

  </body>
</html> 